name: Node.js Build and Deploy

on:
  push:
    branches:
      - feat/cp-build
env:
  REACT_APP_BUILD_TARGET: 'web'
  REACT_APP_AMPLITUDE_KEY: ${{ secrets.REACT_APP_AMPLITUDE_KEY }}
  REACT_APP_AMPLITUDE_URL: ${{ secrets.REACT_APP_AMPLITUDE_URL }}
  DISABLE_ESLINT_PLUGIN: true
  GENERATE_SOURCEMAP: false
  REACT_APP_API_URL: ${{ secrets.API_URL }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'

      - name: Install dependencies
        run: yarn install

      - name: Build project
        run: yarn build-web

      - name: Copy build to remote host
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.DEPLOY_SSH_HOST }}
          username: ${{ secrets.DEPLOY_SSH_USERNAME }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: 'build/'
          target: '/home/ubuntu/hackertab.dev/'
